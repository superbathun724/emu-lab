# emu-lab — CHIP-8 → Game Boy 에뮬레이터 (학습 프로젝트)

## 프로젝트의 목적
이 프로젝트의 목표는 "잘 돌아가는 에뮬레이터"가 아니라, 사용자가 CPU·메모리·기계어의 동작을
직접 구현하면서 이해하는 것이다. 이후 리버스 엔지니어링과 펌웨어 분석으로 이어갈 계획이다.
따라서 속도보다 사용자의 이해가 우선이다.

## 역할 분담 (가장 중요한 규칙)
**사용자가 직접 작성하는 부분 (코어 로직)**
- CPU: fetch / decode / execute, 각 명령어(opcode) 구현
- 메모리 맵, 레지스터, 플래그 처리
- 타이머, 인터럽트의 핵심 로직
- 그래픽 처리(CHIP-8 DXYN, Game Boy PPU)의 핵심 로직

**Claude가 작성해도 되는 부분 (주변부)**
- 빌드 설정(CMake), 프로젝트 구조
- SDL2 창·입력·사운드 등 플랫폼 코드
- 테스트 하네스, 테스트 ROM 자동 실행 스크립트
- 로그·트레이스 출력, hex dump 같은 디버그 도구
- 코드 리뷰, 리팩터링 제안

사용자가 "이 부분은 네가 짜줘"라고 **명시적으로** 요청할 때만 코어 로직을 작성한다.
코어 파일에 코드를 추가해야 할 때는 함수 시그니처와 `// TODO:` 주석만 남긴다.

## 사용자가 코어 로직에서 막혔을 때
바로 정답 코드를 주지 않고 아래 순서로 단계를 올린다.
1. 무엇을 기대했고 실제로 무엇이 일어났는지 되묻는다.
2. 참고해야 할 스펙 문서의 위치를 알려준다 (docs/references.md 기준).
3. 의사코드나 비트 연산 수준의 힌트를 준다.
4. 사용자가 요청하면 그때 코드를 보여주고, 한 줄씩 설명한다.

버그 찾기를 도울 때는 원인을 추측만 하지 말고 재현·확인 방법(로그 추가, 트레이스 비교,
최소 재현 ROM)을 먼저 제시한다.

## 기술 스택과 코딩 규칙
- 언어: C11 / 그래픽·입력·사운드: SDL2 / 빌드: CMake + Ninja
- 환경: Windows. 툴체인은 MSYS2 UCRT64(gcc, cmake, ninja, SDL2)를 사용한다.
  Claude Code는 Git Bash에서 실행되므로 `C:\msys64\ucrt64\bin`이 PATH에 있어야 한다.
- SDL2 외의 외부 라이브러리를 추가하기 전에 사용자에게 확인받는다.
- 크기가 중요한 값은 고정폭 정수(`uint8_t`, `uint16_t`)를 쓴다.
- 전역 변수 대신 상태 구조체(`Chip8`, 이후 `GameBoy`)를 넘긴다.
- `src/chip8/`(코어)는 SDL에 의존하지 않는다. 플랫폼 코드는 `src/platform/`에만 둔다.
  → 나중에 테스트, 디버거, WebAssembly 빌드를 붙이기 위해서다.
- 컴파일 경고는 `-Wall -Wextra`로 켜고, 경고를 남겨두지 않는다.
- 코드 주석은 한국어로 쓴다.

## Windows 주의사항
- 파일은 반드시 바이너리 모드(`"rb"`)로 연다. 텍스트 모드는 0x0D 0x0A 같은 바이트를 바꿔 ROM을 망가뜨린다.
- SDL2의 main 처리: MinGW에서는 `SDL2::SDL2main`을 함께 링크하거나 `SDL_MAIN_HANDLED`를 정의한다.
- 디버그 출력을 보기 위해 콘솔 창을 유지한다(`-mwindows` 쓰지 않음).
- 빌드 후 `SDL2.dll`을 실행 파일 옆으로 복사하는 단계를 CMake에 넣는다.
- 경로는 코드 안에 하드코딩하지 않고, 명령줄 인자로 받는다.

## 빌드와 실행
```
cmake -B build -G Ninja
cmake --build build
./build/emu.exe roms/<파일명>.ch8
```

## 디렉터리 구조
- `src/chip8/` CHIP-8 코어 (사용자 담당)
- `src/platform/` SDL2 플랫폼 코드
- `tests/` 테스트 코드
- `tools/` 디스어셈블러, 덤프 등 보조 도구
- `roms/` 테스트 ROM (git에 올리지 않음)
- `docs/` 로드맵, 참고 자료, 개발 일지, 버그 기록
- `prompts/` Claude Code에 쓴 주요 프롬프트 보관

## 작업 방식
- 파일을 여러 개 바꾸는 작업은 먼저 계획을 보여주고 확인을 받는다.
- 한 번에 한 단계씩 진행하고, 변경 후에는 빌드가 되는지 확인한다.
- 새로운 개념이 나오면 두세 문장으로 설명하고 스펙의 해당 위치를 알려준다.
- `sudo`가 필요한 명령은 직접 실행하지 말고 사용자에게 보여준다.
- 작업 단위가 끝나면 git 커밋 메시지를 제안한다.
- 세션이 끝날 때 `docs/DEVLOG.md`에 들어갈 기록 초안을 제안하되,
  "배운 점" 칸은 사용자가 직접 쓰도록 비워둔다.

## 저작권
상용 게임 ROM을 다운로드하거나 저장소에 포함하지 않는다.
테스트 ROM과 공개 홈브루만 사용한다.

## 현재 진행 상황
`docs/ROADMAP.md`의 체크박스를 기준으로 한다.
